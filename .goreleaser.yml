version: 2

project_name: protopen

gomod:
  dir: ./cli

builds:
  - dir: ./cli
    binary: protopen
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - formats: [tar.gz]
    name_template: "protopen_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

release:
  github:
    owner: samsolomon
    name: protopen-cli
